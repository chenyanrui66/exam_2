# 多残骸音爆定位：联合求解方案（问题二与问题三）

> **说明**：本方案以问题二的理论模型为核心，以问题三的具体数据为算例验证。两部分既相互独立又前后呼应——问题二回答"如何建模、至少需几台设备"，问题三用真实数据证明模型有效、7台设备全部可用，并验证"5秒时间窗"加回后结果不变。

---

## 第一部分：问题二——模型建立与最少设备数分析

### 1.1 问题重述

空中存在 $n=4$ 个残骸，每个残骸在某一时刻 $	au_j$ 于位置 $\mathbf{p}_j=(x_j,y_j,z_j)$ 处产生跨音速音爆。地面布置 $m$ 台监测设备，第 $i$ 台设备在位置 $\mathbf{s}_i$ 处，按时间先后顺序记录到 $n$ 组音爆到达时刻 $\{t_{i1},t_{i2},\dots,t_{in}\}$。**关键难点**：设备不知道收到的第 $k$ 个读数来自哪个残骸，即关联（correspondence）未知。

要求：
1. 建立数学模型，确定各读数与残骸的对应关系；
2. 确定4个残骸的音爆位置和时刻；
3. 分析至少需要多少台监测设备。

### 1.2 核心建模思路

将"多残骸多设备多时刻"的混乱数据，转化为**可解析检验的候选事件**，再通过**全局一致性**筛选真解。流程如下：

$$\text{单事件枚举} \to \text{解析种子生成} \to \text{全站残差验真} \to \text{精确覆盖优化}$$

**关键洞察**：对一个"潜在残骸"，若已知它从每台设备产生的到达时刻（各取一个），则该残骸的时空状态 $\theta=(x,y,z,\tau)$ 可由TOA方程唯一确定。问题转化为：从每台设备的 $n$ 个读数中各选一个，组成 $n^m$ 种"单事件候选"，再从中挑出 $n$ 个互不冲突的候选。

### 1.3 数学模型

#### 1.3.1 TOA方程

对残骸 $j$ 与设备 $i$，音爆传播满足：

$$t_{ik} = \tau_j + \frac{\|\mathbf{p}_j - \mathbf{s}_i\|}{c} + \varepsilon_{ik}$$

其中 $c=340\,\text{m/s}$ 为声速，$\varepsilon_{ik}$ 为测量误差。

#### 1.3.2 一对一关联约束

引入0-1关联变量 $a_{ijk}$，当且仅当设备 $i$ 的第 $k$ 个读数来自残骸 $j$ 时 $a_{ijk}=1$。约束为：

$$\sum_{j=1}^{n} a_{ijk} = 1, \quad \sum_{k=1}^{n} a_{ijk} = 1, \quad a_{ijk} \in \{0,1\}$$

第一式：一个读数不能属于多个残骸；第二式：一台设备对一个残骸只能贡献一次记录。

#### 1.3.3 联合优化目标

$$\min_{A,\Theta} \; Q = \sum_{i=1}^{m} \sum_{j=1}^{n} \sum_{k=1}^{n} a_{ijk} \left[ t_{ik} - \tau_j - \frac{\|\mathbf{p}_j - \mathbf{s}_i\|}{c} \right]^2$$

#### 1.3.4 物理可行性约束

- **因果律**：$\tau_j \le t_{ik}$（音爆必须先于接收发生）；
- **空间边界**：残骸位于监测区域合理范围内，高程 $z_j \ge 0$；
- **时间窗（问题三特有，此处先不用）**：$\max_j \tau_j - \min_j \tau_j \le 5\,\text{s}$。

### 1.4 求解策略：解析种子 + 全站联合验真

直接求解上述混合整数非线性问题属于NP-hard。本文采用**"解析种子 + 组合优化"**的两层策略，将搜索空间从 $(n!)^m$ 压缩到 $n^m$ 次解析求解。

#### 1.4.1 解析种子生成（四站差分法）

对某一单事件候选（从 $m$ 台设备各取一个读数，记为 $\{t_i\}_{i=1}^m$），任选4台设备作"种子站"，其TOA方程为：

$$\|\mathbf{p} - \mathbf{s}_i\|^2 = c^2 (t_i - \tau)^2, \quad i=1,2,3,4$$

将第 $i$ 个方程减去第1个方程，$\|\mathbf{p}\|^2$ 与 $\tau^2$ 两个二次项**同时抵消**：

$$2(\mathbf{s}_i - \mathbf{s}_1)^\top \mathbf{p} = \|\mathbf{s}_i\|^2 - \|\mathbf{s}_1\|^2 - c^2(t_i^2 - t_1^2) + 2c^2\tau(t_i - t_1)$$

这是关于 $\mathbf{p}$ 的线性方程组。三个方程联立，可将位置表示为时刻的线性函数：

$$\mathbf{p}(\tau) = \mathbf{p}_0 + \tau \mathbf{p}_1$$

代回第一个设备的原始方程，得到关于 $\tau$ 的**一元二次方程**，至多两个实根。于是"4站定位"退化为：解一个 $3\times 3$ 线性方程组 + 一个二次方程——**无迭代、无初值依赖、微秒级**。

> **几何意义**：两个球面相交于一个圆，该圆躺在两球面的**根平面**（radical plane）上；三个根平面交于一条直线，音爆点必在此直线上。

#### 1.4.2 全站残差验真

以解析种子为初值，用**全部 $m$ 台设备**做有界Levenberg-Marquardt精化，计算全站RMSE：

$$\text{RMSE} = \sqrt{\frac{1}{m}\sum_{i=1}^{m} \left( t_i - \hat{\tau} - \frac{\|\hat{\mathbf{p}} - \mathbf{s}_i\|}{c} \right)^2}$$

RMSE低于阈值（如 $0.02\,\text{s}$）的候选进入下一层。这一步的鉴别力来自**冗余观测**：错误关联的"鬼影解"不可能让全部设备的预测到达时刻同时撞上真实读数。

#### 1.4.3 精确覆盖（组合优化）

从通过验真的单事件候选中，选出 $n$ 个，使每台设备的 $n$ 个读数被恰好各用一次。即解**精确覆盖问题**（exact cover），目标为最小化联合残差平方和：

$$\min \sum_{j=1}^{n} \text{RMSE}_j^2$$

### 1.5 最少设备数分析（PDOP）

设备数由两个独立维度决定：

| 维度 | 下限 | 说明 |
|:---|:---|:---|
| **可解性** | 4台 | 4个未知量 $(x,y,z,\tau)$，4个方程可解，但无冗余无法验错 |
| **关联冗余** | 5台 | 4台只能解一个残骸，无法区分"正确关联"与"鬼影关联"；第5台提供第一条冗余校验 |

用**几何精度因子**（PDOP, Position Dilution of Precision）量化不同设备组合的定位精度。PDOP定义为位置子矩阵的迹的平方根：

$$\text{PDOP} = \sqrt{\text{tr}\left[(\mathbf{H}^\top \mathbf{H})^{-1}_{1:3,1:3}\right]}$$

其中 $\mathbf{H}$ 为TOA雅可比矩阵。PDOP越小，几何构型越好，定位精度越高。

下表以问题三的设备布局为基准，计算各组合在最坏残骸位置上的PDOP（单位：km/s）：

| 设备数 | 组合 | 最坏PDOP | 平均PDOP | 评价 |
|:---|:---|---:|---:|:---|
| 4 | ABEF | 3.110 | 2.305 | 最低可解配置，无冗余校验能力 |
| 5 | ABDEF | 2.979 | 2.187 | **推荐**：新增一条残差校验，性价比最高 |
| 6 | ABCDEF | 2.954 | 2.059 | 继续改善，边际收益变小 |
| 7 | ABCDEFG | 2.940 | 1.985 | 定位精度和容错最佳 |

> **反例**：ABCG组合虽满秩，但最坏PDOP高达 $24.603\,\text{km/s}$，条件数 $209.85$，几何构型极差，不应使用。

**结论**：
- **理论下限**：4台设备可解出时空坐标（连续维数的最低要求）；
- **工程推荐**：**至少5台**。第5台引入的冗余观测是"鉴别鬼影关联"的第一道可靠防线，使模型从"可解"跃升为"可验证"。

---

## 第二部分：问题三——数据验证与一致性检验

> **说明**：问题三提供了7台设备的具体坐标和28个到达时刻数据。本节用第一部分建立的模型求解，并做三层检验：（1）7站残差诊断证明数据全部可用；（2）验证5秒时间窗加回后结果不变；（3）三重交叉验证。

### 2.1 数据与坐标系

以设备A为参考点，建立局部直角坐标系：
- $x$ 轴朝东：经度差 $\times\, 97.304\,\text{km}/\text{°} = 97304\,\text{m}/\text{°}$；
- $y$ 轴朝北：纬度差 $\times\, 111.263\,\text{km}/\text{°} = 111263\,\text{m}/\text{°}$；
- $z$ 轴朝上：高程直接取米。

所有计算在统一米-秒单位制下进行，声速 $c = 340\,\text{m/s}$。

### 2.2 求解流程与结果

#### 2.2.1 搜索空间压缩

| 计算阶段 | 数量 | 说明 |
|:---|---:|:---|
| 单事件组合总数 | $4^7 = 16384$ | 从7台设备各选一个读数 |
| 物理边界+因果律过滤后 | 1696 | 高程 $\ge 0$、音爆时刻早于所有观测 |
| 全站TOA精化后保留候选 | 4 | RMSE $< 0.02\,\text{s}$ 且最大残差 $< 0.04\,\text{s}$ |
| 精确覆盖组合 | **1** | 4个候选恰好完美覆盖28个读数，无冲突 |

#### 2.2.2 最终解（按音爆时刻排序）

| 残骸 | 经度 (°) | 纬度 (°) | 高程 (km) | 音爆时刻 (s) | 全站RMSE (s) |
|:---:|---:|---:|---:|---:|---:|
| #1 | 110.500001 | 27.309998 | 12.514 | 11.9999 | 0.000157 |
| #2 | 110.499999 | 27.949998 | 11.529 | 13.0014 | 0.000139 |
| #3 | 110.300000 | 27.650000 | 11.478 | 14.0000 | 0.000267 |
| #4 | 110.699999 | 27.650000 | 13.468 | 15.0000 | 0.000134 |

**关联方案**（设备读数按时间先后编号1~4）：

| 残骸 | A | B | C | D | E | F | G |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| #1 | 1 | 2 | 4 | 4 | 3 | 4 | 2 |
| #2 | 4 | 4 | 2 | 1 | 4 | 1 | 3 |
| #3 | 2 | 3 | 3 | 2 | 2 | 2 | 1 |
| #4 | 3 | 1 | 1 | 3 | 1 | 3 | 4 |

### 2.3 7站残差诊断（数据质量自证）

关联确定后，对每个残骸做7站最小二乘精化，逐站残差（实测 $-$ 预测到达时刻）如下（单位：毫秒）：

| 残骸 | A | B | C | D | E | F | G | 最大 $\|\text{残差}\|$ |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| #1 | +0.07 | $-$0.19 | +0.28 | $-$0.21 | +0.06 | +0.04 | $-$0.04 | **0.28** |
| #2 | +0.15 | $-$0.12 | +0.11 | +0.17 | +0.01 | $-$0.12 | $-$0.21 | **0.21** |
| #3 | +0.34 | $-$0.51 | +0.22 | $-$0.15 | +0.10 | +0.15 | $-$0.16 | **0.51** |
| #4 | $-$0.23 | +0.16 | $-$0.16 | $-$0.01 | +0.04 | +0.12 | +0.08 | **0.23** |

**诊断结论**：
- 全部28个残差落在 $\pm 0.6\,\text{ms}$ 以内，RMS仅 $0.18\,\text{ms}$，折合成距离不到 $0.2\,\text{m}$；
- 残差呈**随机抖动**，无系统性符号偏向（如没有某站始终为正或负）；
- 各站最大残差均匀分布，没有某站显著偏大。

这说明**7台设备全部可靠，无需剔除**。冗余度 $r = 7 - 4 = 3$ 全部转化为精度提升，而非用于排除异常站。

> **两道关卡机制**：若某站存在异常，其读数在关联验证阶段就匹配不上任何合理残骸假设的预测；即使混进关联，精化后该站残差也会显著大于其他站。本题两道关卡均平安通过。

### 2.4 5秒时间窗验证

4个音爆时刻为 $11.9999\,\text{s}, 13.0014\,\text{s}, 14.0000\,\text{s}, 15.0000\,\text{s}$，时间跨度：

$$\Delta \tau = 15.0000 - 11.9999 = 3.0001\,\text{s} < 5\,\text{s}$$

**关键说明**：本方案在求解过程中**未使用**5秒限制作为输入约束，但输出结果自然满足该条件。这说明：
1. 问题三的正确解不是由"5秒先验"筛出来的；
2. 即使把5秒限制加回模型作为硬约束，也不会改变结果（因为唯一精确覆盖组合本身就满足）；
3. 模型的鉴别力来自**全部7台设备的联合TOA一致性**，而非时间窗先验。

### 2.5 三重交叉验证

| 验证项 | 结果 | 说明 |
|:---|:---|:---|
| **独占性** | 通过 | 每台设备的4个读数被4个残骸恰好各用一次，无冲突、无遗漏 |
| **时间窗** | 通过 | 跨度 $3.0001\,\text{s} \le 5\,\text{s}$ |
| **物理合理性** | 通过 | 高程 $11.5\sim 13.5\,\text{km}$，正是跨音速段典型高度 |
| **残差水平** | 通过 | 全站RMSE均低于 $0.3\,\text{ms}$，数据高度自洽 |

**几何佐证**：4个音爆点在水平面上呈规则的十字对称分布（东西 $\pm 0.2\text{°}$、南北 $\pm 0.155\text{°}$ 围绕中心 $110.5\text{°}/27.65\text{°}$），时刻为整齐的 $12/13/14/15\,\text{s}$——说明题目数据正是从这组"真值"正向生成的，本方案等于盲解反演出了出题人的设定。

---

## 附录：核心代码

完整Python代码见下载链接，仅依赖 `numpy`，可直接运行。

**代码要点**：
- `analytic_seeds()`：四站差分解析生成种子，无迭代；
- `refine()`：全部7站LM精化，不依赖5秒窗和时差门限；
- `exact_cover()`：组合优化确保一对一覆盖；
- `pdop_analysis()`：几何精度因子计算。

**运行方式**：
```bash
python 问题二三联合求解方案.py
```

输出包括：4个残骸的时空坐标、关联方案、逐站残差、5秒窗验证、PDOP分析表。
